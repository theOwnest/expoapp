import 'package:flutter/material.dart';

const marginH = EdgeInsets.symmetric(
  horizontal: 20,
);
const marginV = EdgeInsets.symmetric(
  vertical: 20,
);
const marginHV20 = EdgeInsets.all(
  20,
);
const marginHV10 = EdgeInsets.symmetric(
  vertical: 10,
  horizontal: 20,
);
const appbarPadding = EdgeInsets.only(
  top: 50,
  bottom: 10,
  left: 20,
  right: 20,
);
const marginHV5 = EdgeInsets.symmetric(
  vertical: 5,
  horizontal: 20,
);
const marginHV8 = EdgeInsets.symmetric(
  vertical: 8,
  horizontal: 20,
);
